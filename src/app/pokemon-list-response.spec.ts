import { PokemonListResponse } from './pokemon-list-response';

describe('PokemonListResponse', () => {
  it('should create an instance', () => {
    expect(new PokemonListResponse()).toBeTruthy();
  });
});
